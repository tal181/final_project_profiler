package com.project.checks.domain

object Constants {
  val Metrics = "metrics"

  // output columns (same as defined in com.intuit.curation.dataQuality.domain.MetricResult.Attributes)
  val MetricNameColumn = "metricName"
  val MetricValueColumn = "value"
}
